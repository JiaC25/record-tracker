namespace RecordTracker.API.Features;

public static class HttpErrorMessage
{
    # region CreateUser
    public const string EMAIL_ALREADY_REGISTERED = "Email is already registered.";
    #endregion
}