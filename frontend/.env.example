# Example environment variables for the Record Tracker frontend

# Base API URL (must end without a trailing slash)
# This should match the backend API URL.
# For local development, you can use: (in .env.local)
# NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_API_URL=